# 2026-08-12 API 对照与模拟器浏览验证

## 用户目标

以单纯浏览为目标验证 APK 的首页和动态详情，并将用户提供的社区 API 整理纳入项目文档参考。

## 已完成

- 在已启动的 `Pixel_10` Android 16 模拟器中安装当前 Debug APK，安装和启动成功。
- 首页头条加载成功；点击真实动态卡片后进入 `FeedActivity`，页面显示“加载失败”。
- 使用临时开启、带脱敏的 HTTP 日志构建确认：同一会话的首页、应用详情和登录检查请求收到 `403 Forbidden`，响应体为空，服务端为 TencentEdgeOne。
- 恢复普通 Debug 构建，确认 `ENABLE_HTTP_BODY_LOGGING = false`，并重新安装到模拟器。
- 对照 [Coolapk-API-Collect](https://github.com/Coolapk-UWP/Coolapk-API-Collect) 的 HTTP 首部、主页 V8 和动态详情资料，补充 `docs/api.md` 的来源、差异和排查顺序。

## 结论

当前“点击内容加载失败”已在模拟器复现，根因范围指向上游请求被拒绝，而不是 APK 签名、安装或模拟器无法联网。动态详情的精确 HTTP 状态码尚未在该次点击中单独采集，因此暂不把“详情接口必然 403”写成已证实事实。

## 未完成

- 未切换 API1/API2 Host。
- 未修改 Token 时间戳算法。
- 未调整匿名 Cookie 或请求头。
- R-002、R-004、R-005、R-013 均未因本次验证自动勾选完成；R-013 的 Base URL 尾部斜杠已通过构建和单元测试，但真实接口仍被 403 阻断。

## 后续

按单变量顺序验证：动态详情 API1 Host → 整数 Unix 秒 Token → 匿名 Cookie/请求头，并保留 HTTP 状态码与脱敏错误体。

## 2026-08-12 主导航悬浮底栏

### 用户目标

将竖屏页面下方的三段式导航按钮改为 Apple 风格的悬浮底栏。

### 修改

- `activity_main.xml` 按 Miuix `FloatingNavigationBar` 的关键参数设置 36dp 外侧留白、8dp 视觉底部间距、`surfaceContainer` 实色背景、1dp 白色细边、28dp 图标和 12sp 标签；后续整改将浮岛背景移到独立容器，导航控件保持透明。
- 参考 iOS 酷安截图保留轻量选中胶囊和圆角浮岛；截图中的五个入口和中央加号没有全部映射到当前应用，因此保留现有首页、消息、设置三段导航，不新增没有业务实现的页面。
- 关闭 Material 默认 active indicator 的绘制，使用自定义 item background 提供低对比选中态；标签始终显示，保留首页、消息、设置的点击行为和消息角标。
- `MainActivity` 将系统导航栏 inset 从底栏内部 padding 改为动态 bottom margin，并让 ViewPager2 预留底栏高度、底部间距和系统栏空间，避免内容穿过浮岛或浮岛覆盖系统手势区。
- 横屏 `NavigationRailView` 未修改。
- 增加/更新底栏主题资源、`docs/todo.md`、`docs/decisions.md` 和本变更记录。

### 验证

- 首次构建因当前 shell 未配置 Java 被环境阻断；改用 Android Studio JDK 21 后继续验证。
- Material 1.10.0 不支持 `itemActiveIndicatorEnabled`，已移除该不兼容属性；保留 `itemActiveIndicatorStyle` 但配置为透明 0dp，由 item background 接管选中态。
- 最终资源调整后 `:app:assembleDebug`：通过。
- 最终资源调整后 `:app:testDebugUnitTest`：通过，4 个测试，0 failures、0 errors、0 skipped。

### 模拟器安装与基础验收

- 设备：`emulator-5554`，Android 16，`sdk_gphone16k_arm64`。
- `adb install -r app/build/outputs/apk/debug/app-debug.apk`：成功。
- `MainActivity` 冷启动：成功，最终安装包 `TotalTime=2415ms`，进程保持运行。
- 最终截图 `/tmp/coolapk-miuix-pass-final.png` 确认实色 Miuix 浮岛、白色细边、左右留白、低对比选中胶囊、内容安全区、底部手势区安全间距和首页选中态正常。
- 最终包执行“消息 → 设置 → 首页”三次切换均成功；UI 层级确认首页节点 `navigation_home` 的 `selected=true`。

### 未完成

- 尚未专项确认深色模式、主题色和滚动隐藏/显示动画。

## 2026-08-12 底栏“我的”与搜索迁移

### 用户目标

将底栏的“设置”改为“我的”，在右侧增加“搜索”，并把首页顶部搜索功能移动到底栏。

### 修改

- `nav_menu.xml` 调整为“首页 / 消息 / 我的 / 搜索”；“我的”使用账号图标，搜索使用搜索图标。
- `SettingsFragment` 保持原有设置内容，仅将导航和页面标题改为“我的”；没有新增未定义的个人中心页面。
- 移除 `fragment_home.xml` 顶部搜索按钮，保留右侧首页菜单。
- `MainActivity` 的底栏搜索项启动现有 `SearchActivity`，并返回 `false` 保持当前页面导航选中态；原有三页 `ViewPager2` 不变。
- 同步 README、context、architecture、development、todo、decisions 和 changelog。

### 验证

- `:app:assembleDebug`：通过。
- `:app:testDebugUnitTest`：通过，6 个测试，0 failures、0 errors、0 skipped。
- `emulator-5554` 安装和冷启动：成功。
- 最终截图 `/tmp/coolapk-four-tab-final.png` 确认底栏四项、顶部搜索移除和内容安全区；`/tmp/coolapk-bottom-search.png` 确认底栏搜索进入原搜索页；`/tmp/coolapk-my-final-2.png` 确认“我的”页面标题和原设置内容。
- UI 层级确认底栏节点包含 `navigation_home`、`navigation_message`、`navigation_me` 和 `navigation_search`；搜索点击后的顶层 Activity 为 `SearchActivity`。

## 2026-08-12 浮岛层级与底部间距整改

### 用户反馈

悬浮底栏后方仍像有一层背景，且底栏距离屏幕底部过远。

### 修改

- `activity_main.xml` 增加 `bottomNavContainer` 作为唯一浮岛背景容器；内部 `BottomNavigationView` 改为透明，只保留导航内容和选中项胶囊。
- `dimens.xml` 将视觉底部间距从 26dp 调整为 8dp。
- `MainActivity` 将系统 inset 和滚动隐藏/显示行为绑定到竖屏浮岛容器；横屏继续使用 `NavigationRailView`，不套用竖屏容器行为。

### 验证

- `:app:assembleDebug`：通过。
- `:app:testDebugUnitTest`：通过，6 个测试，0 failures、0 errors、0 skipped。
- 最新 APK 已安装到 Android 16 `emulator-5554`；主界面冷启动成功。
- UI 层级确认浮岛容器边界为 `[95,2172][985,2340]`，首页/关注/我的切换和底栏搜索均正常。
- 截图 `/tmp/coolapk-floating-nav-patched-home.png`、`/tmp/coolapk-floating-nav-patched-follow-2.png` 确认底栏只保留一层浮岛背景，底部距离已收紧。

## 2026-08-12 API 身份修复、同类仓库对照与浏览复验

### 修改

- 参考 [Coolapk-API-Collect](https://github.com/Coolapk-UWP/Coolapk-API-Collect) 的 HTTP 首部、主页 V8 和动态详情资料，将首页与动态详情切换到 API1。
- 将 `X-App-Token` 的时间戳从 Float 秒改为整数 Unix 秒，并接通已有的自定义 Token 设置：仅在开关开启且值非空时覆盖自动生成 Token。
- 停止在启动时把 `/v6/apk/detail` 返回的线上应用版本写入第三方客户端的请求 Header，保持默认 `13.4.1/2312121` 请求身份稳定。
- 只读克隆 [HDYOU/c001apk](https://github.com/HDYOU/c001apk) 快照进行对照：其已注释掉远端版本写回逻辑，但仍保留 Float Token 和 API2 首页/详情路由，因此未直接复制。

### 验证

- `:app:testDebugUnitTest`：6 个用例，0 failures、0 errors、0 skipped。
- `:app:assembleDebug`：通过；正常 Debug 包的 `ENABLE_HTTP_BODY_LOGGING=false`。
- Android 16 `emulator-5554` 清空测试包状态后，应用详情、首页 V8、动态详情 `id=73186553` 和动态评论均返回 `200 OK`。
- UIAutomator 确认首页头条显示真实动态正文；点击后 `FeedActivity` 显示“动态”和完整动态正文，未再出现“加载失败”。

### 结论

问题不是单纯缺少 `X-App-Token`：请求原本已经发送该 Header，但 Float 时间戳、错误的 API2 路由以及自动写入线上应用版本共同造成拒绝。当前匿名浏览最小链路已在模拟器通过；登录、发布、图片、真实设备和 Release 仍未完成验收。

## 2026-08-12 Android 16 16 KB 页面大小兼容性修复

### 用户目标

修复打开应用时 Android 16 16 KB 模拟器提示应用不兼容的问题。

### 复现与定位

- 设备为 `emulator-5554` / `Pixel_10`，系统页大小 `16384`。
- 旧包冷启动时出现 `PageSizeMismatchDialog`，提示 `lib/arm64-v8a/libpl_droidsonroids_gif.so` 的 `RELRO segment not aligned`。
- ELF 检查确认问题集中在旧 GIF AAR 内置的 `libpl_droidsonroids_gif.so`，不是普通 APK zip 对齐问题。

### 修改

- `gradle/libs.versions.toml`：AGP 升至 8.5.1，GIF 依赖切换到 `pl.droidsonroids.gif:android-gif-drawable:1.2.32`。
- `SketchImageViewLoader`：移除旧 Sketch GIF 的 BuildConfig 依赖和 protected bitmap hook，改用新库公开的当前帧 API；普通图片 bitmap pool 路径未改。
- `app/build.gradle.kts`：为 instrumentation 回归测试增加 test-only GIF 依赖。
- 新增 `GifNativeCompatibilityTest`，用最小 GIF 实际创建并释放 `GifDrawable`。
- 同步更新 README、context、development、decisions、todo 和 changelog。

### 验证

- `:app:assembleDebug`、`:app:testDebugUnitTest`、`:app:bundleDebug`：通过。
- Debug APK：`zipalign -P 16` 和 native ELF 对齐检查通过。
- Debug AAB：`bundletool dump config` 显示 `PAGE_ALIGNMENT_16K`；Bundle 生成的 device APK 对齐通过。
- `:app:connectedDebugAndroidTest`：Pixel_10 上 2 个测试通过。
- APK 与 Bundle 生成的 APK 均在 16 KB 模拟器冷启动成功，无 `PageSizeMismatchDialog`、`RELRO` 或 native 崩溃日志，`pageSizeCompat=0`。

### 边界

Release 签名、真实设备、完整人工图片业务路径和 GIF bitmap pool 的性能变化仍未专项验收；没有通过兼容性开关压制提示。

## 2026-08-12 本地关注与“我的”二级设置

### 用户目标

关注页默认为空；话题和数码不分 Tab，用户在指定话题/数码详情页关注后，以本地关注形式保留并合并展示。我的页面包含本地收藏、浏览历史和设置，原消息页内容不再作为主导航页面。

### 修改

- 新增 `LocalFollow`、`LocalFollowDao`、`LocalFollowDatabase` 和 `LocalFollowRepo`，以 `(type, targetId)` 复合主键保存本地话题/数码关注。
- `LocalFollowFragment` 替换主导航中的原消息页：无记录显示空状态，有记录显示统一列表；支持打开详情和删除本地关注。
- `TopicViewModel` 和 `TopicFragment` 将话题/数码详情页关注动作先写入本地数据库；登录时再尽力同步服务端，未登录时不依赖登录状态。
- `MyFragment` 保留本地收藏、浏览历史，并新增 `SettingsActivity` 二级设置入口；设置项继续由原 `SettingsPreferenceFragment` 承载。
- 同步 README、context、architecture、development、todo、decisions 和 changelog。

### 验证

- `:app:assembleDebug`：通过。
- `:app:testDebugUnitTest`：通过，6 个测试，0 failures、0 errors、0 skipped。
- `adb install -r app-debug.apk`：成功；Android 16 `emulator-5554` 启动成功。
- 模拟器已验证关注空状态、话题 Android 关注、数码“小米17 Pro Max”关注、应用重启后持久化与两类记录合并展示；关注页不再显示原消息内容。
- `git diff --check`：通过。

### 边界

本地关注数据库与服务端关注状态是两条独立状态：本地动作先落库，详情页在登录状态下再尝试同步服务端，服务端接口失败时不应被解释为本地数据库失败。真实设备、不同账号和 Release 包尚未专项验收。

## 2026-08-12 本地关注头像补充

- 根因：`LocalFollow` 原先只保存类型、目标 ID、标题，详情返回的 `logo` 没有进入本地记录，列表只能显示类型图标。
- 修改：新增 `avatar` 字段和 Room 1→2 迁移；详情页关注时保存 `logo`，列表使用现有图片加载器展示，旧记录无头像时保留类型图标兜底；重新打开旧详情页时会补齐头像。

## 2026-08-12 底栏隐藏后的内容截短修复

### 用户反馈

底栏按钮滚动隐藏后，内容下方仍出现一整块空白区域，像是有一层不可见背景遮挡内容。

### 定位与修改

- 设备复现确认：修复前 `ViewPager2` 内部内容列表固定到约 `2172px`，底栏隐藏后仍保留“底栏高度 + 视觉间距 + 系统 inset”的约 `252px` 底部 padding。
- `MainActivity` 移除对 `ViewPager2` 的永久底部 padding；浮岛继续通过独立容器承载背景、系统 bottom margin 和滚动隐藏/显示行为。
- 列表自身的 `fitsSystemWindowsInsets` 与末尾 footer 继续保留，负责系统手势区和滚动到末尾时的安全距离。

### 验证

- `:app:assembleDebug`：通过。
- `:app:testDebugUnitTest`：通过，6 个测试，0 failures、0 errors、0 skipped。
- Debug APK：安装到 Android 16 `emulator-5554` 成功，`MainActivity` 冷启动成功。
- 修复后上滑隐藏底栏，内容延伸到屏幕高度 `2424px`，原空白遮挡区消失；下滑恢复底栏，浮岛仍正常显示并保持手势区间距。
